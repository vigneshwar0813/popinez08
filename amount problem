amount=int(input("Enter amount the rupees:"))
notes=[500,200,100,50,20,10,5,2,1]
dict={}
for i in notes:
    if (amount>=i):
        count=amount//i
        dict[i]=count
        amount-=count*i
print (dict)
    
